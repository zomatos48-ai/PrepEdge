package com.prepedge.dto.request;

import lombok.Data;

@Data
public class StartMockTestRequest {
    // intentionally empty — mock test ID comes from the path variable
    // this exists as a placeholder for future fields (e.g. language preference)
}